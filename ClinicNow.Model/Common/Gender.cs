namespace ClinicNow.Model.Common;

/// <summary>A patient's gender, shown in the medical record header. A fixed, code-controlled set - not a user-editable reference table.</summary>
public enum Gender
{
    Male = 0,
    Female = 1,
    Other = 2
}
