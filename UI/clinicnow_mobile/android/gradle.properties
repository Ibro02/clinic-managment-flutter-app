org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Added by the Flutter template, and it must stay false for now.
#
# The build warns that firebase_core applies the Kotlin Gradle Plugin and that
# "future versions of Flutter will fail to build" because of it. That warning is
# real but not yet actionable here: setting this to true was tried and the build
# fails during configuration, because moving to AGP 9's built-in Kotlin is a
# whole-project migration (Flutter's own template files and every plugin
# subproject), not one property.
#     See https://docs.flutter.dev/release/breaking-changes/migrate-to-built-in-kotlin
#
# So the mitigation is a pin, not a fix: this app builds on Flutter 3.44.9, and
# the Android build should not be upgraded past it before submission. Revisit
# once the FlutterFire plugins have completed the migration upstream.
android.builtInKotlin=false
